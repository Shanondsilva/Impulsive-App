# Core

Contains shared infrastructure used across Impulsive, including constants, utilities, dependency injection, result wrappers, and time abstractions.

Core should stay generic and avoid feature-specific business logic.
