# Frontend

Contains all UI-facing code for Impulsive, including navigation, screens, reusable components, themes, and previews.

Frontend code should render state, handle UI events, and delegate business behavior to backend use cases or session coordinators.
