# Copy this file to keystore.properties and fill in real release signing values.
# Do not commit keystore.properties or the keystore file.

storeFile=path/to/release.keystore
storePassword=replace-with-store-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
